# 93. Market Research V6 Catalyst Supported Carry Persistence Refresh Plan

## Mission
Draft a narrow `v6` manifest that keeps the training-gap objective primary while using catalyst context only as bounded symbol-selection support.

## In Scope
- Freeze a new reference base.
- Freeze a new seed universe.
- Freeze manifest entries for `2` true carry rows and `2` clean persistence rows.

## Out Of Scope
- Bootstrapping the v6 dataset.
- Reopening exhausted local replay.
- Promoting catalyst context into the mainline.

## Success Criteria
- Produce a manifest artifact that is ready to bootstrap.
- Keep opening clones excluded.
- Keep catalyst context explicitly support-only.

## Stop Criteria
- The criteria are not frozen.
- The manifest overlaps the combined reference base.

## Handoff Condition
- If the manifest is green, the next legal action is the v6 bootstrap chain.

## 2026-03-30 local status
- `600118 / q3 / C` is the first bounded v6 lane and closed as opening-led, not true carry.
- Local second-lane expansion is now blocked by `v12_v6_first_lane_phase_check_v1.json`.
- `v6` remains active, but current posture is `keep_v6_as_active_but_hold_local_second_lane_after_opening_first_lane`.

## 2026-03-30 waiting-state transition
- `v12_v6_first_lane_phase_check_v1.json` and `v12_v6_reassessment_v1.json` both held local widening closed.
- `v12_waiting_state_summary_v1.json` therefore enters explicit waiting state instead of forcing `v7`.
