# V1.12AO CPO Role-Layer Patch Pilot

- Mission:
  - patch the weakest current layer, `role_state_label`, before widening geometry
  - keep the experiment report-only
- Patch scope:
  - preserve the same `7` truth rows and the same time split from `V1.12AM`
  - add only bounded market-observable microstructure and behavior features
- Current working assumption:
  - secondary extension confusion is partly an A-share microstructure problem, not only a pure optical-link business-role problem
