# V1.12AC CPO Unsupervised Role-Challenge Probe

## Goal
- Challenge the current manual `core / adjacent / branch / spillover / pending` cohort grammar with a bounded unsupervised probe.
- Use only contemporaneously visible `stage-window` and `evidence-axis` structure.
- Keep all outputs review-only.

## Why This Phase Exists
- The current `CPO` line already has:
  - a bounded cycle reconstruction
  - a bounded cohort map
  - a bounded labeling surface review
- But manual role language still contains subjective judgment.
- This phase tests whether data-side latent structure:
  - supports that manual grammar
  - or challenges specific boundaries

## Governance Boundary
- No formal role replacement.
- No formal label freeze.
- No formal training rights.
- No automatic promotion from cluster to label.

## Acceptance Standard
- The probe is useful if it produces:
  - supportive latent structure
  - challenging latent structure
  - review-only candidate signatures
- The probe fails if it becomes stealth relabeling.
