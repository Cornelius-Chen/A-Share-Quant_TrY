# V1.12AZ CPO Bounded Training Layer Extension

- Mission:
  - extend the bounded training-facing layer from `7` rows to `10` rows
  - add only the `3` guarded branch rows approved by `V1.12AY`
- Boundary:
  - no formal training
  - no formal signal generation
  - connector/MPO branch stays outside
