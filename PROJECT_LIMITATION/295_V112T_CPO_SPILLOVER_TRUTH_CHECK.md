# V1.12T CPO Spillover Truth-Check

`V1.12T` is the third precise cleaning pass after adjacent cleanup and chronology normalization.

Its job is not deletion. Its job is classification:

- which weak-relevance rows may preserve A-share-specific spillover information
- which weak-relevance rows mostly look like name bonus or pure board-follow behavior
- how to keep those rows in review memory without letting them silently pollute the core cohort
