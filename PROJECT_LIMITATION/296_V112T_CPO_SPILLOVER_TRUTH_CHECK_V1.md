# V1.12T CPO Spillover Truth-Check V1

This pass reviewed the `3` frozen mixed-relevance spillover rows from the CPO registry.

Current outcome:

- preserved as possible A-share spillover factor candidates:
  - `000070`
  - `603228`

- preserved as name-bonus or pure board-follow candidate:
  - `001267`

The key point is not that these rows became cleaner business-relevance objects. The key point is that they are no longer a flat noise bucket.
